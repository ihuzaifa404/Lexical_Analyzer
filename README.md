# Lexical_Analyzer
A lexical analyzer developed for Compiler Construction that scans source code, identifies tokens (keywords, identifiers, operators, literals, and symbols), and performs the first phase of compilation by converting input text into a stream of tokens.
